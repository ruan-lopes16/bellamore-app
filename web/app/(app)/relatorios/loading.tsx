import RelatoriosSkeleton from './RelatoriosSkeleton';

/**
 * Skeleton de navegação de Relatórios. Mesma definição que o `page.tsx` usa no
 * gate de fetch (`RelatoriosSkeleton.tsx`) — não podem divergir.
 */
export default function RelatoriosLoading() {
  return <RelatoriosSkeleton />;
}
