import FinanceiroSkeleton from './FinanceiroSkeleton';

/**
 * Skeleton de navegação do Financeiro. Mesma definição que o `page.tsx` usa no
 * gate de fetch (`FinanceiroSkeleton.tsx`) — não podem divergir.
 */
export default function FinanceiroLoading() {
  return <FinanceiroSkeleton />;
}
